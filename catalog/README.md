# Каталог ИИ-бизнесов мира: что есть и чего нет в России

Широкий каталог бизнесов, построенных на ИИ или вокруг ИИ, работающих в мире по
состоянию на август 2026 года, с проверкой российского аналога по каждой идее — по
таксономии из 23 семей. Задание, из которого собран каталог, —
[`../prompt/PROMPT.md`](../prompt/PROMPT.md). Предыдущий прогон этого же проекта, узкий
и только по потребительским нишам рунета, — [`../research/`](../research/).

## Итог в одну строку

**439 карточек и 433 различимых идей (6 карточек — повтор идеи из другой семьи), 23 семьи по таксономии (26 файлов — семьи 1, 4 и 5 разбиты на подсемьи A/B). По России: 199 частично, 147 занято, 62 свободно, 31 нет данных.**

«Свободно» и «нет данных» — не то же самое, что подтверждённая пустота рынка: почему и
как это читать правильно — в разделе «Ограничения данных» ниже.

## Метод

Как каталог собирался фактически:

1. **26 агентов каталога** — по одному на каждый файл семьи, собирают идеи по
   обязательным полям (`buyer`, `players`, `money`, `moat`, `ru_analog`, `kill_switch`,
   `failures` и другие) с цифрой, источником и датой к каждому заявлению.
2. **Скептик на каждую семью** — отдельный проход поверх готовой карточки: атака на
   несущие цифры и на вердикты по России, поиск опровержения и уточнения. Сырой
   результат — `data/proverka.json`, свод — `40-proverka.md`.
3. **Сведение проверки с карточками.** Результат проверки внесён в сами карточки:
   поле `proverka` показывает, что именно изменила проверка, поле `ru_verdict` хранит
   итоговый вердикт по России. Плоский индекс и машинное оглавление пересчитываются из
   карточек скриптом `site/reindex.py` — расхождение между ними невозможно.

По десяти семьям скептик проходил дважды, и заходы иногда расходятся в выводе. Оба
сохраняются в карточке, а итоговым берётся более «занятый»: найденный игрок опровергает
пустоту ниши, а его ненахождение вторым проверяющим её не доказывает.

## Как устроена карточка

Помимо полей задания (`buyer`, `players`, `money`, `pricing`, `moat`, `capital`,
`time_to_revenue`, `foreign_dependency`, `ru_analog`, `ru_fit`, `kill_switch`,
`failures`) в карточке есть служебные поля:

| Поле | Что означает |
|---|---|
| `ru_verdict` | Итоговый вердикт по России: занято, частично, свободно, нет данных. Единственный источник истины для индексов и сайта |
| `proverka` | Что изменила адверсариальная проверка: прежний и новый вердикт, найденный российский игрок, опровергнутые и уточнённые цифры с доказательством |
| `duplicate_of` | Карточка повторяет идею из другой семьи; в счёт различимых идей входит основная |
| `see_also` | Смежная карточка: общие игроки, но другой предмет покупки или покупатель |

## Как читать папку

| Файл | Что внутри |
|---|---|
| [00-index.md](00-index.md) | Плоский индекс всех карточек одной таблицей: id, название, семья, вердикт по России, суть одной фразой, самая говорящая цифра. Быстрее, чем открывать по очереди 26 карточных файлов. Собирается из карточек скриптом, вручную не правится |
| [families/*.md](families/) | 26 файлов с карточками по семьям (23 семьи, три разбиты на подсемьи A/B). В каждом — интро семьи, собственный мини-индекс, полные карточки идей, раздел «Провалы и закрытия», раздел «Что не проверено и почему» и раздел с правками проверки, не привязанными к отдельной карточке |
| [40-proverka.md](40-proverka.md) | Свод адверсариальной проверки: проверенные утверждения (подтверждено / уточнено / опровергнуто / не проверяемо) и оспоренные вердикты по России. Сами правки внесены в карточки, этот файл — сводка по ним |
| `90-provaly.md` | Сводная таблица провалов из всех 26 семейных файлов: 234 строки по типу исхода — банкротство и закрытие 48, продажа за бесценок 15, поглощение команды без продукта 72, тихое затухание 99. Плюс раздел «Базовые ставки» (7 сквозных показателей и порядка 76 отдельных процентных цифр с источником) и короткий синтез по кластерам. Собран из семейных разделов: три отдельные целевые охоты за провалами не выполнялись |
| `99-neproverennoe.md` | 329 пунктов непроверенного по всем 26 семьям. Первым разделом — фазы, которые не выполнялись, и чем это ослабляет вердикты; отдельным разделом — 18 случаев, где данных нет в принципе |
| [data/index.json](data/index.json) | Машиночитаемое оглавление всех 26 семей: `family`, `file`, `ideas[{id, name, one_liner, ru, key_number}]`. Производный файл, собирается из карточек скриптом `site/reindex.py` |
| [data/proverka.json](data/proverka.json) | Сырой журнал адверсариальной проверки: правки к цифрам и вердиктам с результатом проверки и доказательством к каждой. Внесены в карточки скриптом `site/apply_proverka.py` |
| [data/raw-journal.jsonl](data/raw-journal.jsonl) | Сырой журнал вызовов агентов прогона — события `started`/`result` без обработки, 131 запись |

## Ограничения данных

- Прогон остановлен на 93% недельного лимита. Из пяти запланированных сквозных
  проходов по российским каналам выполнен один; не выполнены четыре: сторы и мобильные
  приложения; vc.ru, Habr, Product Radar; CNews, TAdviser, госзакупки, интеграторы;
  каталоги телеграм-ботов и VK Mini Apps. Не выполнены и три отдельные охоты за
  провалами.
- Из-за этого вердикты «свободно» держатся только на том, что нашли или не нашли
  агент семьи и скептик, — не на отдельной целенаправленной проверке по российским
  каналам сбыта, а по семи семьям (15, 17, 18, 19, 20, 21, 23) — и вовсе без скептика.
  В предыдущем прогоне (`../research/`) именно такой целевой проход — по сторам и
  каталогам — убил 6 кандидатов из 8, уже прошедших ту же первичную проверку. А там,
  где скептик всё же прошёл по вердиктам заново (138 штук, свод в `40-proverka.md`),
  133 изменились, и ни разу — в сторону «свободнее», только в сторону «более занято».
  Поэтому вердикт «свободно» в этом каталоге правильно читать как «не найдено при
  нынешней глубине поиска», а не как доказанную пустоту рынка.
- Финансовые показатели западных игроков в основном сторонние оценки (Sacra, Latka,
  PitchBook, Tracxn, Zylos Research и подобные), а не первичная отчётность; карточки
  отмечают это явно там, где источники расходятся.
